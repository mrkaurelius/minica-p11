package p11signer

import (
	"testing"
)

func TestInternal(t *testing.T) {
	// key := getPublicKey("testkey")
	// fmt.Println(key)

	// signature := sign([]byte("merhabayalandunya"))
	// fmt.Printf("signature: %x", signature)
	// Finalize()
}
